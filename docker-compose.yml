web:
  build: .
  command: python manage.py runserver 0.0.0.0:8000
  volumes:
    - .:/app
  ports:
    - "8000:8000"


# build_app:
#   build: .
#   volumes:
#     - .:/app
#   command: python manage.py startapp testimonies


# testserializer:
#   build: .
#   command: python manage.py shell
#   volumes:
#     - .:/app
